const addCommentScene = require('./addComment');
const searchNumberScene = require('./searchNumber');
const writeDeveloperScene = require('./writeDeveloper');

module.exports = {
  addCommentScene,
  searchNumberScene,
  writeDeveloperScene
};